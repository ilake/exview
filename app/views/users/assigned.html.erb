<ul>
  <% @assigneds.each do |assign| %>
    <li>
      <%= link_to_user_profile(assign.receiver) %>
      <%= link_to "share photo", new_photo_path(:receiver_id => assign.receiver) %>
      <% if assign.sent_at %>
        share photo last time at <%= assign.sent_at %>
      <% else %>
         waitting your photo
      <% end %>
    </li>
  <% end %>
</ul>
